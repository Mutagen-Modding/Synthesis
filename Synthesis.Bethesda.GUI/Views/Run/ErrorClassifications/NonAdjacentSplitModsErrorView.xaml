<UserControl x:Class="Synthesis.Bethesda.GUI.Views.ErrorClassifications.NonAdjacentSplitModsErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:common="clr-namespace:Synthesis.Bethesda.GUI.Views.Common"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,0,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Error Type Header -->
        <TextBlock Grid.Row="0"
                   Text="{Binding ErrorType}"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{StaticResource SalmonBrush}"
                   Margin="0,0,0,10" />

        <!-- Error Message with Read More Button -->
        <StackPanel Grid.Row="1"
                    Margin="0,0,0,15">
            <TextBlock Text="{Binding Message}"
                       FontSize="14"
                       TextWrapping="Wrap"
                       Foreground="White"
                       Margin="0,0,0,10" />

            <common:ReadMoreButton Command="{Binding ReadMoreCommand}" />
        </StackPanel>

        <!-- Base Mod Info -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Base Mod:"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{StaticResource SalmonBrush}"
                       Margin="0,0,0,5" />
            <TextBlock Text="{Binding BaseModKey}"
                       FontSize="14"
                       FontWeight="Bold"
                       FontFamily="Consolas"
                       Margin="10,0,0,0" />
        </StackPanel>

        <!-- Split Mods Header -->
        <TextBlock Grid.Row="3"
                   Text="Split Files That Must Be Adjacent:"
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="{StaticResource SalmonBrush}"
                   Margin="0,0,0,5" />

        <!-- Split Mods List -->
        <ItemsControl Grid.Row="4"
                      ItemsSource="{Binding SplitMods}"
                      Margin="0,0,0,15">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="10,2">
                        <Run Text="{Binding ModKey, Mode=OneWay}"
                             Foreground="White"
                             FontFamily="Consolas" />
                    </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Load Order Header -->
        <TextBlock Grid.Row="5"
                   Text="Load Order at Time of Error:"
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="{StaticResource SalmonBrush}"
                   Margin="0,0,0,5" />

        <!-- Load Order List -->
        <ScrollViewer Grid.Row="6"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding LoadOrder}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="5,2">
                            <Run Text="{Binding ModKey, Mode=OneWay}"
                                 Foreground="White"
                                 FontFamily="Consolas" />
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
